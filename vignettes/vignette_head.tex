\usepackage{mathtools}

% A starred version \abs*{} is produces delimiters that are preceded by \left
% and \right, or use \abs[\Bigg]{\frac{a}{b}}
\DeclarePairedDelimiter\abs{\lvert}{\rvert}
\DeclarePairedDelimiter\norm{\lVert}{\rVert}
\DeclareMathOperator{\cv}{cv} % formerly tilde chi
\DeclareMathOperator{\se}{se} % standard error
\DeclareMathOperator{\sd}{sd} % standard deviation
\newcommand{\hatse}{\widehat{\se}} % standard error estimator
\DeclareMathOperator{\cov}{cov} %
